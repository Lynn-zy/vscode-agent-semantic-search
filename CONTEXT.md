# Agent 语义检索中继

本扩展是一个中继层：它不实现检索，而是把 VS Code Agent 的语义检索请求透传给底层 Copilot 服务，再把多态返回值规整为模型可读的文本。本上下文描述这条中继链路上的语言。

## Language

**语义检索（Semantic Search）**：
按功能意图或概念描述检索工作区代码、返回相关代码片段的检索方式，区别于关键词精确匹配。
_Avoid_: 智能检索、AI 搜索、模糊搜索

**代码库索引（Codebase Semantic Index）**：
工作区代码经向量化后形成的索引。它是语义检索的前提：代码库索引构建完成之后，语义检索才能返回结果。由底层 Copilot 构建，本扩展只负责触发构建与收集诊断。
_Avoid_: 语义索引、向量索引、知识库

**中继（Relay）**：
本扩展的核心职责：把 Agent 的检索请求透传给底层服务，再把底层返回的多态内容规整为 Markdown。
_Avoid_: 桥接、转发、代理

**中继目标（Relay Target）**：
被中继调用指向的底层检索工具，默认 `copilot_searchCodebase`；按候选列表顺序解析，并永久排除本扩展自身。
_Avoid_: 底层工具、上游、后端

**限定目录（Scoped Directories）**：
入参，用于把检索范围收敛到工作区的部分路径。接受绝对路径、工作区相对路径与 glob 模式，超出数量上限时截断。
_Avoid_: 搜索范围、过滤条件

**检索结果（Search Outcome）**：
一次语义检索请求的终态，三选一：命中、无匹配、失败。
_Avoid_: 响应、返回值

**检索失败（Relay Failure）**：
一次语义检索未能产出结果。涵盖中继前的入参校验失败、中继目标缺失、超时、未就绪与底层异常。
_Avoid_: 报错、错误

**未就绪（Not Ready）**：
底层语义检索当前不可用。成因可能是代码库索引尚未构建，也可能是底层服务未启用。
_Avoid_: 索引缺失、服务不可用

**降级（Degradation）**：
检索无匹配或失败时，引导 Agent 改用确定性工具（`grep_search` / `file_search`）继续推进，而不是反复重试语义检索。
_Avoid_: 兜底、回退、fallback

**降级提示（Degradation Hint）**：
承载降级的文本，分两部分：原因段说明为什么会这样，建议段说明下一步该做什么。
_Avoid_: 错误信息、提示语

**自引用（Self-Reference）**：
中继目标解析必须排除本扩展自身注册的工具，否则请求会递归回到自己。
_Avoid_: 死循环、递归调用
