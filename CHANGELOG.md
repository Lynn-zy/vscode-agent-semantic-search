# 更改日志 (Change Log)

本项目的所有重要更改均将记录在此文件中。

版本格式遵循 [Semantic Versioning](https://semver.org/) 规范。

---

## [0.1.4] - 2026-09-23

### 优化与改进 (Improved)

- **工具模型提示词优化（确立首选探索优先级）**：优化了工具的元数据描述，移除容易导致智能体过度避让的负向限制语句，确立其作为工作区代码库探索首选语义检索工具的定位，显著提升 Agent 主动调用 `semanticSearch` 检索业务逻辑与功能概念的积极性；
- **文档规范化**：工具能力对比矩阵与使用说明中，符号引用分析工具的名称统一对齐 VS Code 原生 `vscode_listCodeUsages` 标准。

---

## [0.1.3] - 2026-09-23

### 新增特性 (Added)

- **符号引用追踪协同指引 (`vscode_listCodeUsages`)**：在工具模型描述中增补与符号引用工具的协同指引，帮助智能体（Agent）在通过语义搜索定位到代码后，能自主调用 `vscode_listCodeUsages` 追踪函数的调用方和改动影响面；
- **提示开关配置项**：新增 `semanticSearch.appendCodeUsagesHint` 配置项（默认开启），在成功检索到代码后自动在结果末尾附带符号引用查询提示，并支持用户在设置中一键开启或关闭。

### 文档完善 (Documentation)

- 在使用说明文档（README）中增补语义检索与符号引用追踪的标准协作工作流与配置说明。

---

## [0.1.2] - 2026-09-23

### 修复与优化 (Fixed)

- **修复超长检索结果写入临时文件的缺陷**：修复了当检索出的代码片段较多时，结果会被 VS Code 自动转储为 `content.txt` 临时文件、导致 Agent 无法直接查看代码内容的缺陷。现在大段代码切片能够直接完整输出在对话上下文中，使用体验与官方原生检索保持一致。

### 文档与规范 (Documentation)

- 参考 VS Code 官方规范，系统完善工作区代码语义检索的核心定位、工作流程以及与其他检索工具的能力对比矩阵。

---

## [0.1.1] - 2026-09-22

### 修复与优化 (Fixed)

- **优化命令面板展示**：修复命令在 VS Code 命令面板中分类前缀重复显示的问题，命令标题更简洁清晰；
- **完善多语言国际化**：扩展设置项与错误提示弹窗全面支持简体中文与英文界面；
- **优化排查与降级引导**：当检索无匹配或底层服务不可用时，向 Agent 提供更规范的排查建议，引导其转用文本搜索或文件名搜索继续推进任务；
- **增强状态栏稳定性**：修复索引构建过程中若发生异常可能导致状态栏状态无法正常复位的问题。

---

## [0.1.0] - 2026-09-22

### 新增特性 (Added)

- **工作区代码语义检索 (`semanticSearch`)**：让使用第三方模型（如 DeepSeek、Claude、Gemini 等）的 Agent 能够自主进行工作区代码语义检索，无需用户在聊天框手动键入 `#codebase`；
- **无损代码切片透传**：完整保留检索到的相关代码片段及行号信息，避免代码被截断；
- **智能降级防死循环**：在代码库索引未构建或底层检索不可用时，自动向 Agent 提示原因并引导改用确定性工具，避免陷入重试死循环；
- **状态栏一键管理**：在右下角状态栏实时显示语义搜索状态，提供构建代码库索引与查看诊断信息的一键入口。
