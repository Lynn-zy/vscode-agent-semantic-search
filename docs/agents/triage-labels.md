# Triage 标签

各技能以五个「标准 triage 角色」表述。本文件把这些角色映射到本仓库 issue tracker 实际使用的标签字符串。

| mattpocock/skills 中的标签 | 本仓库 tracker 中的标签 | 含义                               |
| -------------------------- | ----------------------- | ---------------------------------- |
| `needs-triage`             | `needs-triage`          | 维护者需要评估该 issue             |
| `needs-info`               | `needs-info`            | 等待报告者补充信息                 |
| `ready-for-agent`          | `ready-for-agent`       | 已完整规格化，可直接交给 AFK agent |
| `ready-for-human`          | `ready-for-human`       | 需要人类实现                       |
| `wontfix`                  | `wontfix`               | 不予处理                           |

当技能提到某个角色（例如「apply the AFK-ready triage label」）时，使用本表中对应的标签字符串。

如需改用其他词汇，直接编辑右列。
