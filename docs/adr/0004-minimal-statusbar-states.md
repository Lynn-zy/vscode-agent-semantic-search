# 状态栏收敛为极简的双状态模型

状态栏曾短暂维护过 `ok`、`empty`、`failed`、`idle`、`indexing` 五种状态。我们决定将状态栏精简收敛为仅保留 `idle`（就绪）与 `indexing`（构建中）两种状态。因为单次语义检索的无匹配或偶发超时属于瞬态调用结果，并不能代表工作区代码库索引的真实构建状况；且底层 Copilot 未开放索引就绪与过时事件的订阅 API，剥离单次检索结果的瞬态反馈能彻底避免状态栏频繁闪烁和误导用户。
